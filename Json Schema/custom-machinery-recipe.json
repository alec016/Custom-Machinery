{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://alec016.github.io/Custom-Machinery/Json%20Schema/custom-machinery-recipe.json",
  "type": "object",
  "$defs": {
    "craft": {
      "$schema": "./src/main/resources/schemas/craft_recipe.json"
    },
    "machine": {
      "$schema": "./src/main/resources/schemas/machine_recipe.json"
    }
  }
  "patternProperties": {
    "^.+$": {
      "type": "object",
      "allOf": [
        {
          "if": {
            "required": ["output"]
          },
          "then": {
            "$ref": "#/$defs/craft"
          }
        },
        {
          "if": {
            "required": ["time"]
          },
          "then": {
            "$ref": "#/$defs/machine"
          }
        }
      ]
    }
  }
}
